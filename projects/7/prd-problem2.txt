In this problem, you will learn to determine the optimal number of topics for your STM model using the `searchK()` function from the `stm` package. This analysis will help you identify the most appropriate number of topics by evaluating metrics like semantic coherence and exclusivity.

Please follow these steps:

(a)	Use the `searchK()` function from the `stm` package on your preprocessed documents to test a range of topics (e.g., 2 to 6). Use `Rating` as a covariate.

(b)	Plot the results generated by `searchK()` to visualize exclusivity vs. semantic coherence.

(c)	Rescale (normalize) both the coherence and exclusivity scores to the same range (e.g., 0 to 1).

(d)	Next, calculate and plot a composite score for Coherence and Exclusivity using their rescaled (normalized) values as follows:

Composite Score = 0.5 × (Rescaled Coherence) + 0.5 × (Rescaled Exclusivity)

(e)	Plot the composite score versus the number of topics, and justify your choice of the optimal number of topics based on this composite score.

(f)	For each of your topics, present
•	The top 7 words
•	The word cloud, where word size reflects its prevalence in the topic
•	The top review most associated with the topic
